{
  "name": "logging-middleware",
  "version": "1.0.0",
  "description": "Reusable logging package that ships log events to the evaluation Test Server",
  "main": "src/index.js",
  "scripts": {
    "test": "echo \"no tests\" && exit 0"
  },
  "dependencies": {
    "axios": "^1.15.2",
    "dotenv": "^16.3.1"
  }
}
